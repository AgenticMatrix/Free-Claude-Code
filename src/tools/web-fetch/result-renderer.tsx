import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * WebFetch results are rendered inline in the tool-use block.
 */
export function WebFetchResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}

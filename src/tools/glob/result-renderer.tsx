import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * Glob results are rendered inline in the tool-use block.
 */
export function GlobResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}

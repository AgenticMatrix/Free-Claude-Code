import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * TaskStop results are rendered inline in the tool-use block.
 */
export function TaskStopResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}

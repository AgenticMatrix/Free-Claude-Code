import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * TaskOutput results are rendered inline in the tool-use block.
 */
export function TaskOutputResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}

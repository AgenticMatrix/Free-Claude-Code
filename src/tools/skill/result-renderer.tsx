/**
 * Skill result renderer — returns null (results shown inline in the tool-use block).
 */

import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

export function SkillResultRenderer(
  _props: ToolResultRendererProps,
): React.ReactNode {
  return null;
}

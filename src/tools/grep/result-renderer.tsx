import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * Grep results are rendered inline in the tool-use block.
 */
export function GrepResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}

import React from 'react';
import type { ToolResultRendererProps } from '../types.js';

/**
 * WebSearch results are rendered inline in the tool-use block.
 */
export function WebSearchResultRenderer(_props: ToolResultRendererProps): React.ReactNode {
  return null;
}
